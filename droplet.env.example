DROPLET_IP=152.42.247.86
DROPLET_USER=root
LOCAL_API_PORT=9000

# This file configures the local SSH tunnel only. Compact Polymarket evidence
# is configured separately in /etc/price-collector/collector.env on the droplet.
# Review the POLYMARKET_EVIDENCE_* keys in deployment/collector.env.example;
# POLYMARKET_EVIDENCE_ENABLED=false is the default. See OPERATIONS.md before
# manually enabling it and restarting price-collector-polymarket-probabilities.
# Keep writer credentials out of this file and /etc/price-collector/api.env.

